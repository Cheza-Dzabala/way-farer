class User {
    constructor({id, email, password, is_admin, first_name, last_name})
    {
        this.id = id;
        this.email = email;
        this.password = password;
        this.is_admin = is_admin;
        this.first_name = first_name;
        this.last_name = last_name;
    }
}